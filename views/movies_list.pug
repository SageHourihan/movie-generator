extends layout

block content
    h1= title

    ul(class='mlist')
        each movie in movie_list
            li
                a(href=movie.url) #{movie.title}
        else
            li There are no movies.